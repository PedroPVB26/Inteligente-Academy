spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/inteligenteacademy
    username: utfpr
    password: utfpr
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: inteligenteacademy.dev@gmail.com
    password: ojne cchz rskk tqxy

    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 3000
          writetimeout: 5000
        debug: true

server:
  port: 8081